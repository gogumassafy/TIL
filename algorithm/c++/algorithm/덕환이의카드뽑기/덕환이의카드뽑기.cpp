#include <stdio.h>
using namespace std;

int T, result, N, K;
long long arr[]

int main() {
	scanf("%d", &T);
	for (int tc = 1; tc <= T; ++tc) {
		scanf("%d %d", &N, &K);
		

		printf("#%d %d", tc, result);
	}

}