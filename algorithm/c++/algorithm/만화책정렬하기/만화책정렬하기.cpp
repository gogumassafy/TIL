#include <iostream>
using namespace std;

int T, N, result;

int main() {
	scanf("%d", &T);
	for (int tc = 1; tc < T + 1; ++tc) {
		result = 0;
		scanf("%d", &N);
		for (int i = 0; i < N; ++i) {

		}
		printf("#%d %d", tc, result);
	}
	return 0;
}