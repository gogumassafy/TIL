#include <stdio.h>
#include <vector>
using namespace std;

int T;
vector<int> treeHeight;

int main() {
	scanf("%d", &T);
	for (int tc = 1; tc <= T; ++tc) {
		int N, M;
		scanf("%d %d", &N, &M);
		for (int i = 0; i < N; ++i) {

		}
		printf("#%d %d", tc, result);
	}
	return 0;
}