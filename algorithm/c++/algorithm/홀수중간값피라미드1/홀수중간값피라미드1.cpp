#include <stdio.h>

int T, N, X, result;

int dfs();

int main() {
	scanf("%d", &T);
	for (int tc = 1; tc <= T; ++tc) {
		scanf("%d %d", &N, &X);
		result = 0;

		

		printf("#%d %d", tc, result);
	}
	return 0;
}

int dfs() {

}