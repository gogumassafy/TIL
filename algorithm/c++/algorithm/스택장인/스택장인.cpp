#include <stdio.h>
using namespace std;

int T;
int N;
int arr[100000];
int stack[100000];


int main() {
	scanf("%d", &T);
	for (int tc = 1; tc <= T; ++tc) {
		scanf("%d", &N);
		for (int i = 0; i < N; ++i) {
			scanf("%d", &arr[i]);
		}
		while (1) {
			
		}
	}
	return 0;
}