#include <stdio.h>
#include <vector>
using namespace std;

struct Node {
    int depth;
};
vector<Node> v;
int N, arr[20][20];

int main() {
    scanf("%d", &N);
    for (int i = 0; i < N; ++i) {
        for (int j = 0; j < N; ++j) {
            scanf("%d", &arr[i][j]);
        }
    }
    v
    return 0;
}